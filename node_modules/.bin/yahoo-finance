#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/-S" ]; then
  exec "$basedir/-S" node --experimental-json-modules --experimental-vm-modules "$basedir/../yahoo-finance2/bin/yahoo-finance.js" "$@"
else 
  exec -S node --experimental-json-modules --experimental-vm-modules "$basedir/../yahoo-finance2/bin/yahoo-finance.js" "$@"
fi
